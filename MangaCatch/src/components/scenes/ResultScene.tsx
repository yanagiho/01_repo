import React from 'react';

export const ResultScene: React.FC = () => {
    return (
        <div style={{ position: 'absolute', inset: 0, display: 'flex', justifyContent: 'center', alignItems: 'center', background: 'rgba(0,0,0,0.9)', fontSize: '8rem' }}>
            FINISH!
        </div>
    );
};
